

<div class="container d-flex flex-wrap  w-100 mt-5">
    <div class="w-50 mt-5">
        @{Html.RenderAction("PVTotTrasgr");}
    </div>
    <div class="w-50 mt-5">
        @{Html.RenderAction("PVPtTrasgr");}
    </div>
    <div class="w-50 mt-5">
        @{Html.RenderAction("PVOverTenPoints");}
    </div>
    <div class="w-50 mt-5">
        @{Html.RenderAction("PVCostoMaggiore");}
    </div>
</div>
